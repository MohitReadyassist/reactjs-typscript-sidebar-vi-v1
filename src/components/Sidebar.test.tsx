import React from "react";
import Sidebar from './Sidebar';
import '@testing-library/jest-dom/extend-expect';

describe("<Sidebar />", () => {
  test("should display a SidebarNav with checked by default", async () => {

  });
});