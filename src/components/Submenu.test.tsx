import  Submenu from './Submenu';
import '@testing-library/jest-dom/extend-expect';

describe("<Submenu />", () => {
  test("should display a SidebarLink, with remember  by default", async () => {
  });
});
